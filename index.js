import {AppRegistry} from 'react-native';
import App from './App.windows';

AppRegistry.registerComponent('billiards_management', () => App);
AppRegistry.registerComponent('billiardsgrade', () => App);